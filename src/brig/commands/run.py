"""
Run command - creates and starts a new cell.

This module contains the cmd_run function which is the main entry point
for creating new cells. It handles:
- Cell definition file parsing
- Image signature verification
- Network setup
- Container creation with gVisor runtime
- Policy configuration
"""

# Note: This is a stub. The full implementation remains in brig.py
# until the incremental migration is complete.

def cmd_run(args):
    """Run a new cell. See brig.py for implementation."""
    raise NotImplementedError("Use brig.py directly until migration is complete")
